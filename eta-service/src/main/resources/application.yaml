server:
  port: 8085

spring:
  application:
    name: eta-service

  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USER}
    password: ${RABBITMQ_PASSWORD}

  data:
    redis:
      host: localhost
      port: 6379